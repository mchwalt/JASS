Echo effect.

- **TIME** — delay length (short = slapback, long = distinct echoes).
- **FB** — feedback: how many repeats (higher = more, can build up).
- **MIX** — echo level against the dry signal.
