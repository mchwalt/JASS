# DemoPresets

Version-controlled example presets that showcase JASS features. Unlike the user's
working presets (which live in `%AppData%\Roaming\Synthy\Presets`), these ship with
the repository so they can be shared and re-seeded.

To try one now, copy the `.synthy` file into `%AppData%\Roaming\Synthy\Presets\` and
pick it from the preset menu in the app.

- **Matrix Demo.synthy** — shows the modulation matrix (Story 8.1): the LFO, the
  Envelope and Velocity all drive the filter Cutoff at once (stacking). Play the
  keyboard with varying strength to hear Velocity and the filter envelope; the drone
  shows the LFO wah.

> TODO (optional): seed these into the user's Presets folder on first run if missing,
> so they ship to every user automatically.
